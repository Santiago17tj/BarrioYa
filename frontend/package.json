{
  "name": "barrioya-frontend-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Static file server adapter for BarrioYa. Serves /app (HTML/CSS/JS) on port 3000 for the Emergent preview environment. The real frontend lives at /app (root).",
  "scripts": {
    "start": "npx --yes serve /app -l tcp://0.0.0.0:3000 --no-clipboard --single"
  }
}
